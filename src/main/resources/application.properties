# Spring Boot application properties
spring.jpa.hibernate.ddl-auto=update

spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
spring.datasource.url=jdbc:h2:file:~/mydb;AUTO_SERVER=TRUE;DB_CLOSE_ON_EXIT=TRUE;DB_CLOSE_DELAY=0
server.shutdown=graceful
spring.lifecycle.timeout-per-shutdown-phase=20s
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# Ensure Hibernate can pick a dialect even when JDBC metadata is unavailable at startup
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# jackson
#spring.jackson.deserialization.fail-on-missing-creator-properties=true